---
title: "BIM·포인트클라우드·360영상 통합 검토 뷰어"
date: 2026-06-12
description: "Navisworks 모델(NWD 변환), 레이저스캔 포인트클라우드, 360° 카메라 영상을 한 화면에 겹쳐 보는 웹 기반 검토 도구"
categories:
  - 프로젝트
tags:
  - BIM
  - 포인트클라우드
  - 360카메라
  - 시공검토
---

현장 시공 검토를 위해 **3D 모델 · 포인트클라우드 · 360° 영상**을 한 화면에 겹쳐 보는
Cupix 스타일의 웹 뷰어를 만들었습니다. 설치 없이 브라우저에서 바로 실행되고,
모든 파일은 내 컴퓨터 안에서만 처리됩니다(서버 업로드 없음).

👉 **[뷰어 실행하기](/viewer/)**

## 주요 기능

- **3D 모델**: IFC·GLB·glTF·FBX·OBJ 로드 (단위 변환, Z-up 보정, 투명도)
- **객체 속성**: IFC 모델은 객체를 클릭하면 속성세트(Pset)까지 표시 — Revit에서 IFC로 내보내면 BIM 속성이 그대로 유지됩니다
- **포인트클라우드**: LAS·PLY·PCD·XYZ 로드, 대용량 자동 표본추출(400만 점), 점 크기·투명도 조절
- **360° 영상**: equirectangular 영상을 구체에 투영, 시점 진입 후 드래그로 둘러보기, 방위 정렬
- **촬영 경로 자동 이동**: 시간대별 웨이포인트를 기록하거나 SLAM 궤적(TUM 형식)을 가져오면 재생에 맞춰 촬영 위치가 자동으로 따라갑니다
- **정렬 도구**: 레이어별 이동·회전·스케일 기즈모 + 수치 입력, 정렬값 JSON 저장/불러오기
- **검토 도구**: 거리 측정, 수평 단면, 화면 캡처, 드래그&드롭 로드

## NWD 파일은 어떻게 여나요?

NWD는 오토데스크 독점 포맷이라 브라우저에서 직접 열 수 없습니다.
Navisworks에서 **파일 → 내보내기 → FBX**로 내보낸 뒤 불러오면 됩니다.
단, FBX에는 형상·이름만 담기고 BIM 속성은 빠집니다.
**객체 속성까지 검토하려면 원본 모델(Revit 등)에서 IFC로 내보내세요** —
객체를 클릭하면 속성세트가 패널에 표시됩니다.

포인트클라우드도 RCP/RCS·E57·LAZ는 [CloudCompare](https://www.cloudcompare.org/)(무료)에서
LAS 또는 PLY로 변환하면 됩니다.

## 사용 흐름 (Cupix식 비교 검토)

1. 모델과 스캔 데이터를 불러온다 (드래그&드롭 가능)
2. `정렬` 패널에서 포인트클라우드를 모델 좌표에 맞춘다 → JSON으로 저장
3. 360 영상을 불러오고 `현재 시점에 배치` → 촬영 위치로 이동, `방위 정렬`로 방향을 맞춘다
4. `시점 진입` 후 **투명도 슬라이더**로 영상↔모델↔스캔을 블렌딩하며 시공 오차를 확인한다
5. 거리 측정·단면·캡처로 검토 결과를 기록한다

## 촬영 위치 자동 계산 (Cupix의 SLAM 대신)

Cupix는 영상에서 카메라 이동 경로를 자동 계산(SLAM)합니다. 이 뷰어는 두 가지 방법을 지원합니다:

1. **수동 웨이포인트** — 영상을 멈추고 그 시점의 촬영 위치로 구체를 옮긴 뒤 `현재 위치 기록`.
   몇 개만 찍어두면 재생 시 사이 구간을 보간해 자동 이동합니다.
2. **SLAM 궤적 자동 계산** — 저장소의
   [`tools/slam`](https://github.com/trier89/trier89.github.io/tree/main/tools/slam) 파이프라인을 쓰면
   `./slam360.sh 현장촬영.mp4` 한 줄로 궤적 파일이 나옵니다 (Docker 필요, 모든 처리는 내 PC에서만).
   결과를 뷰어로 가져오면 경로가 표시되고, 정렬 기즈모로 모델 좌표에 맞추면
   (단안 SLAM은 스케일 보정 필요) 재생과 동기화되어 움직입니다.
